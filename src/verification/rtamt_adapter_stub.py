from __future__ import annotations


class RtamtMonitorStub:
    def evaluate(self, rows: list[dict]) -> None:
        raise NotImplementedError(
            "RTAMT is an optional future replacement for the internal MVP runtime monitor."
        )

